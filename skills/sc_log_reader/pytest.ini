[pytest]
testpaths = tests
# Run from wingman-ai root: PYTHONPATH=skills\sc_log_reader python -m pytest skills\sc_log_reader\tests
# The wingman-ai root must be on sys.path so the conftest can load (api module needed by sc_log_reader __init__)
pythonpath = .
addopts = --import-mode=importlib
